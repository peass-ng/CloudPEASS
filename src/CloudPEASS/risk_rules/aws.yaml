## Rule schema version (used by humans; runtime currently expects v1)
version: 1

## Cloud provider these rules apply to
provider: aws

# NOTE: This file is the source of truth for regex/exact-based risk classification.
# The full per-permission categorized YAML lists (aws_permissions_cat.yaml, etc) are kept for reference
# but should not be used by runtime code.

## =====================
## CRITICAL RULES
## =====================

## Exact action names that must always be classified as `critical`.
## Matching is case-insensitive in the classifier.
critical_exact:
  - acm:ExportCertificate
  - aps:PutResourcePolicy
  - appstream:CreateImageBuilderStreamingURL
  - athena:CreatePresignedNotebookUrl
  - athena:StartCalculationExecution
  - backup:PutBackupVaultAccessPolicy
  - codebuild:StartCommandExecution
  - codeartifact:GetAuthorizationToken
  - cognito-identity:UpdateIdentityPool
  - docdb-elastic:UpdateCluster
  - dsql:PutClusterPolicy
  - dynamodb:PutResourcePolicy
  - ec2:AssociateIamInstanceProfile
  - ec2:GetPasswordData
  - ec2:ModifyInstanceAttribute
  - ec2:ReplaceIamInstanceProfileAssociation
  - ec2-instance-connect:SendSSHPublicKey
  - elasticfilesystem:DeleteFileSystemPolicy
  - elasticfilesystem:PutFileSystemPolicy
  - entityresolution:PutPolicy
  - ecr-public:GetAuthorizationToken
  - ecr-public:SetRepositoryPolicy
  - ecr:GetAuthorizationToken
  - ecs:ExecuteCommand
  - eks-auth:AssumeRoleForPodIdentity
  - es:UpdateDomainConfig
  - events:RetrieveConnectionCredentials
  - gamelift:GetComputeAccess
  - gameliftstreams:CreateStreamSessionAdminShell
  - grafana:CreateWorkspaceApiKey
  - grafana:CreateWorkspaceServiceAccountToken
  - glue:RunStatement
  - iam:CreateAccessKey
  - iam:PassRole
  - iot:OpenTunnel
  - kms:CreateGrant
  - kms:Decrypt
  - kms:PutKeyPolicy
  - kafka:UpdateSecurity
  - lambda:UpdateFunctionCode
  - lambda:UpdateFunctionConfiguration
  - lightsail:CreateBucketAccessKey
  - lightsail:DownloadDefaultKeyPair
  - lightsail:GetInstanceAccessDetails
  - lightsail:UpdateBucket
  - rds:ModifyDBInstance
  - redshift:GetClusterCredentials
  - redshift:GetClusterCredentialsWithIAM
  - rolesanywhere:UpdateTrustAnchor
  - s3:PutBucketPolicy
  - s3:PutAccessPointPolicy
  # S3 boundary-changing ACL operations (treat as critical via exact match)
  - s3:PutBucketAcl
  - s3:PutObjectAcl
  - s3:PutObjectVersionAcl
  - s3tables:PutTableBucketPolicy
  - s3tables:PutTablePolicy
  - sagemaker:CreatePresignedNotebookInstanceUrl
  - sagemaker:UpdateNotebookInstanceLifecycleConfig
  - secretsmanager:GetSecretValue
  - secretsmanager:PutResourcePolicy
  - sns:SetTopicAttributes
  - ssm:GetParameter
  - ssm:GetParameters
  - ssm:GetParametersByPath
  - ssm:ResumeSession
  - ssm:SendCommand
  - ssm:StartAutomationExecution
  - ssm:StartSession
  - sqs:SetQueueAttributes
  - sts:GetDelegatedAccessToken
  - sts:GetServiceBearerToken

## =====================
## EVIDENCE-BACKED DOWNGRADES
## =====================

## Exact actions that are only medium in isolation. Their documented companion
## permissions can still form high-impact combinations.
medium_override_exact:
  # Hosted MCP actions only gate tool discovery/invocation. Live calls were
  # delegated as the caller and still enforced every underlying ECS/EKS/IAM
  # or Kubernetes permission; public knowledge tools expose no account data.
  - ecs-mcp:InvokeReadOnlyTools
  - ecs-mcp:UseMcp
  - eks-mcp:CallPrivilegedTool
  - eks-mcp:CallReadOnlyTool
  - eks-mcp:InvokeMcp
  # These EFS actions do not authorize a mount by themselves. Live tests
  # required ClientMount for write/root/POSIX access-point effects; the network
  # operations have separate documented EC2 dependencies and still need client access.
  - elasticfilesystem:ClientRootAccess
  - elasticfilesystem:ClientWrite
  - elasticfilesystem:CreateAccessPoint
  - elasticfilesystem:CreateMountTarget
  - elasticfilesystem:ModifyMountTargetSecurityGroups
  # PutMedia can inject media into a known active feed, and UpdateDictionary
  # can replace a known vocabulary. They affect integrity but do not expose
  # generated metadata or dictionary entries without separate read actions.
  - elemental-inference:PutMedia
  - elemental-inference:UpdateDictionary
  # Modify can alter rows, but every standard Keyspaces driver also needs
  # cassandra:Select on the read-only system* resources to authenticate.
  # That tested pair is High; Modify alone is not usable through the CQL API.
  - cassandra:Modify
  # The caller also needs iam:PassRole, a capable restore role, and access to
  # the restored target before this becomes a sensitive-data path.
  - backup:StartRestoreJob
  # These become a critical launch-role escalation only as a pair, and only
  # when the caller controls a product artifact associated with a privileged
  # launch constraint. Neither action is independently a privilege grant.
  - servicecatalog:CreateProvisioningArtifact
  - servicecatalog:ProvisionProduct
  # A recipe must track a wildcard version of the attacker-writable component,
  # and its existing pipeline instance profile must have useful privileges.
  # The pair is critical; neither action provides that profile by itself.
  - imagebuilder:CreateComponent
  - imagebuilder:StartImagePipelineExecution
  # CreateDeployment enforces the two companion CodeDeploy actions. The chain
  # is critical only when the caller controls a revision and an existing
  # EC2/on-premises group targets a useful instance profile.
  - codedeploy:CreateDeployment
  - codedeploy:RegisterApplicationRevision
  # A trusted target resource policy and controllable event bus/rule are also
  # required. The pair can invoke privileged code; neither action does so alone.
  - events:PutEvents
  - events:PutTargets
  # No supported caller-facing API exposes connection credentials directly.
  - events:RetrieveConnectionCredentials
  # Both operations accept replacement code, but the service performs the
  # update in the caller's context. Live tests required the generated Lambda
  # preparation actions and iam:PassRole before attacker code ran under the
  # existing canary execution role.
  - synthetics:StartCanaryDryRun
  - synthetics:UpdateCanary
  # UpdateExperimentTemplate retained the existing experiment role without a
  # caller iam:PassRole check, but the tested escalation also required
  # StartExperiment and a pre-authorized FIS-to-SSM role chain. Start alone
  # can only execute the template already stored. The pair is Critical.
  - fis:UpdateExperimentTemplate
  - fis:StartExperiment
  # Upload credentials are issued only while a known build is INITIALIZED.
  # A live READY-build request was rejected, so this cannot replace an
  # immutable build or execute code without a later fleet/deployment action.
  - gamelift:RequestUploadCredentials
  # Returns queued/in-progress IoT job execution metadata but never the job
  # document. Live validation kept it below the three document-returning APIs.
  - iotjobsdata:GetPendingJobExecutions
  # Returns stream-key identifiers but never the secret value. It is useful
  # reconnaissance for the separately High Get/BatchGet actions.
  - ivs:ListStreamKeys
  # Returns the complete AMB billing token used instead of SigV4 for public
  # blockchain-node API requests. Live validation showed credential exposure,
  # but the token provides billable public-chain access rather than private
  # tenant data or AWS privilege escalation.
  - managedblockchain:GetAccessor
  # Returns public on-chain transaction data supplied by hash/ID. It is a
  # billable authenticated query, not account-owned sensitive data.
  - managedblockchain-query:GetTransaction

## =====================
## HIGH RULES
## =====================

## Exact action names that must always be classified as `high`.
high_exact:
  - airflow-serverless:GetTaskInstance
  - airflow-serverless:GetWorkflow
  - airflow-serverless:GetWorkflowRun
  - account:GetContactInformation
  - amplify:GetApp
  - amplify:GetArtifactUrl
  - amplify:GetJob
  - apigateway:GET
  - apigateway:PATCH
  - athena:GetQueryExecution
  - appconfig:GetHostedConfigurationVersion
  - appsync:GraphQL
  - appsync:ListApiKeys
  - appstream:CreateStreamingURL
  - cassandra:Select
  - autoscaling:DescribeLaunchConfigurations
  - backup:DeleteRecoveryPoint
  - batch:DescribeJobDefinitions
  - bedrock:Retrieve
  - bedrock-agentcore:GetWorkloadAccessTokenForUserId
  - b2bi:GetProfile
  - b2bi:GetTransformer
  - cloudformation:DescribeStacks
  - cloudformation:DescribeChangeSet
  - cloudformation:GetTemplate
  - cloudformation:GetTemplateSummary
  - cloudformation:ListExports
  - cloudfront:GetDistribution
  - cloudfront:GetDistributionConfig
  - cloudfront:GetFunction
  - cloudfront:ListDistributions
  - cloudwatch:GetDashboard
  - cloudtrail:LookupEvents
  - ce:GetCostAndUsage
  - budgets:ViewBudget
  - aws-portal:ViewBilling
  - aws-marketplace:SearchAgreements
  - aws-marketplace:DescribeAgreement
  - aws-marketplace:GetAgreementTerms
  - codebuild:BatchGetBuilds
  - codebuild:BatchGetProjects
  - codeartifact:GetPackageVersionAsset
  - codecommit:GetBlob
  - codecommit:GetCommit
  - codecommit:GetFile
  - codecommit:GitPull
  - codepipeline:PollForJobs
  - chime:GetChannelMessage
  - chime:ListChannelMessages
  - connect:BatchDescribeDataTableValue
  - connect:EvaluateDataTableValues
  - connect:GetAttachedFile
  - connect:GetContactAttributes
  - connect:GetFederationToken
  - cognito-idp:AdminGetUser
  - cognito-idp:DescribeUserPoolClient
  - cognito-idp:DescribeIdentityProvider
  - cognito-idp:ListUsers
  - cognito-idp:ListUsersInGroup
  - dax:BatchGetItem
  - dax:GetItem
  - dax:Query
  - dax:Scan
  - devicefarm:GetRemoteAccessSession
  - devicefarm:ListArtifacts
  - dynamodb:BatchGetItem
  - dynamodb:GetItem
  - dynamodb:Query
  - dynamodb:Scan
  - dynamodb:TransactGetItems
  - deadline:AssumeQueueRoleForRead
  - deadline:AssumeQueueRoleForUser
  - datazone:GetEnvironmentCredentials
  - datasync:StartTaskExecution
  - dms:ModifyEndpoint
  - dsql:DbConnect
  - dsql:DbConnectAdmin
  - ec2-instance-connect:OpenTunnel
  - ec2-instance-connect:SendSerialConsoleSSHPublicKey
  - elasticfilesystem:ClientMount
  - elemental-inference:ExportDictionaryEntries
  - elemental-inference:GetMetadata
  - ec2:DescribeLaunchTemplateVersions
  - ec2:DescribeInstanceAttribute
  - elasticache:ModifyUser
  - elasticloadbalancing:ModifyListener
  - ebs:GetSnapshotBlock
  - ecr:GetDownloadUrlForLayer
  - ecs:DescribeTaskDefinition
  - ecs:DescribeTasks
  - emr-serverless:GetApplication
  - emr-serverless:GetJobRun
  - events:ListTargetsByRule
  - firehose:UpdateDestination
  - frauddetector:GetEvent
  - gameliftstreams:GetStreamSession
  - gameliftstreams:GetStreamUrl
  - gameliftstreams:ListStreamUrls
  - globalaccelerator:UpdateEndpointGroup
  - execute-api:Invoke
  - glue:GetConnection
  - glue:GetJob
  - glue:GetWorkflowRunProperties
  - geo:GetDevicePosition
  - geo:GetDevicePositionHistory
  - greengrass:GetComponentVersionArtifact
  - healthlake:ReadResource
  - healthlake:SearchEverything
  - healthlake:SearchWithGet
  - healthlake:SearchWithPost
  - imagebuilder:GetComponent
  - iot:GetThingShadow
  - iot:Publish
  - iot:StartCommandExecution
  - iotjobsdata:DescribeJobExecution
  - iotjobsdata:StartNextPendingJobExecution
  - iotjobsdata:UpdateJobExecution
  - ivs:BatchGetStreamKey
  - ivs:CreateStreamKey
  - ivs:GetStreamKey
  - ivs:UpdateChannel
  - lakeformation:PutDataLakeSettings
  - m2:GetSignedBluinsightsUrl
  - mediaconnect:AddFlowOutputs
  - mediaconnect:DescribeFlowSourceThumbnail
  - mediaconnect:UpdateFlowOutput
  - medialive:DescribeThumbnails
  - medialive:UpdateChannel
  - mediapackagev2:PutChannelPolicy
  - mediapackagev2:PutOriginEndpointPolicy
  - notifications:GetManagedNotificationEvent
  - notifications:ListManagedNotificationEvents
  - mq:CreateUser
  - mq:UpdateUser
  - memorydb:UpdateUser
  - mediapackage:RotateChannelCredentials
  - mediapackage:RotateIngestEndpointCredentials
  - neptune-db:GetStreamRecords
  - neptune-db:ReadDataViaQuery
  - neptune-graph:ReadDataViaQuery
  - iotwireless:GetWirelessDevice
  - iotsitewise:BatchGetAssetPropertyAggregates
  - iotsitewise:BatchGetAssetPropertyValue
  - iotsitewise:BatchGetAssetPropertyValueHistory
  - iotsitewise:GetAssetPropertyAggregates
  - iotsitewise:GetAssetPropertyValue
  - iotsitewise:GetAssetPropertyValueHistory
  - iotsitewise:GetInterpolatedAssetPropertyValues
  - ivschat:CreateChatToken
  - kinesis:GetRecords
  - kinesisanalytics:DescribeApplication
  - kinesisvideo:GetClip
  - kinesisvideo:GetDASHStreamingSessionURL
  - kinesisvideo:GetHLSStreamingSessionURL
  - kinesisvideo:GetImages
  - kinesisvideo:GetMedia
  - kinesisvideo:GetMediaForFragmentList
  - lambda:GetFunction
  - lambda:GetFunctionConfiguration
  - lambda:GetLayerVersion
  - logs:FilterLogEvents
  - logs:GetLogRecord
  - logs:GetLogEvents
  - logs:GetQueryResults
  - medical-imaging:GetImageFrame
  - medical-imaging:GetImageSetMetadata
  - omics:GetReadSet
  - invoicing:BatchGetInvoiceProfile
  - invoicing:GetInvoicePDF
  - invoicing:ListInvoiceSummaries
  - pipes:DescribePipe
  - profile:SearchProfiles
  - rds-db:connect
  - route53:ChangeResourceRecordSets
  - route53domains:GetDomainDetail
  - rum:GetAppMonitorData
  - sdb:GetAttributes
  - sdb:Select
  - s3:GetDataAccess
  - s3vectors:GetVectors
  - s3express:CreateSession
  - sagemaker:DescribeModel
  - sagemaker:DescribeTrainingJob
  - scheduler:GetSchedule
  - servicediscovery:RegisterInstance
  - ses:GetSuppressedDestination
  - ses:GetEmailTemplate
  - ses:ListSuppressedDestinations
  - secretsmanager:PutSecretValue
  - secretsmanager:UpdateSecret
  - signer:StartSigningJob
  - sns:ListSubscriptions
  - sns:ListSubscriptionsByTopic
  - sqs:ReceiveMessage
  - ssm:GetDocument
  - ssm:GetOpsItem
  - ssm:GetParameterHistory
  - states:DescribeStateMachine
  - states:DescribeExecution
  - states:GetActivityTask
  - states:GetExecutionHistory
  - sts:GetFederationToken
  - tax:GetTaxRegistration
  - tax:ListTaxRegistrations
  - textract:GetDocumentAnalysis
  - textract:GetDocumentTextDetection
  - textract:GetExpenseAnalysis
  - transcribe:GetTranscriptionJob
  - transfer:ImportSshPublicKey
  - translate:GetParallelData
  - translate:GetTerminology
  - vpc-lattice-svcs:Invoke
  - wisdom:GetContent

## Evidence-backed corrections for stale upstream/cache entries. These take
## precedence over critical_exact and low_exact during classification.
high_override_exact:
  - account:GetContactInformation
  - amplify:GetApp
  - amplify:GetArtifactUrl
  - amplify:GetJob
  - apigateway:GET
  - apigateway:PATCH
  - athena:GetQueryExecution
  - appconfig:GetHostedConfigurationVersion
  - appsync:GraphQL
  - appsync:ListApiKeys
  - appstream:CreateStreamingURL
  - cassandra:Select
  - autoscaling:DescribeLaunchConfigurations
  - backup:DeleteRecoveryPoint
  - batch:DescribeJobDefinitions
  - bedrock:Retrieve
  - bedrock-agentcore:GetWorkloadAccessTokenForUserId
  - b2bi:GetProfile
  - b2bi:GetTransformer
  - cloudformation:DescribeStacks
  - cloudformation:DescribeChangeSet
  - cloudformation:GetTemplate
  - cloudformation:GetTemplateSummary
  - cloudformation:ListExports
  - cloudfront:GetDistribution
  - cloudfront:GetDistributionConfig
  - cloudfront:GetFunction
  - cloudfront:ListDistributions
  - cloudtrail:LookupEvents
  - ce:GetCostAndUsage
  - codeartifact:GetAuthorizationToken
  - codeartifact:GetPackageVersionAsset
  - codecommit:GetBlob
  - codecommit:GetCommit
  - codecommit:GetFile
  - codepipeline:PollForJobs
  - chime:GetChannelMessage
  - chime:ListChannelMessages
  - connect:BatchDescribeDataTableValue
  - connect:EvaluateDataTableValues
  - connect:GetAttachedFile
  - connect:GetContactAttributes
  - connect:GetFederationToken
  - cognito-idp:AdminGetUser
  - cognito-idp:DescribeUserPoolClient
  - cognito-idp:DescribeIdentityProvider
  - cognito-idp:ListUsers
  - cognito-idp:ListUsersInGroup
  - dax:BatchGetItem
  - dax:GetItem
  - dax:Query
  - dax:Scan
  - devicefarm:GetRemoteAccessSession
  - devicefarm:ListArtifacts
  - dynamodb:BatchGetItem
  - dynamodb:GetItem
  - dynamodb:Query
  - dynamodb:Scan
  - dynamodb:TransactGetItems
  - deadline:AssumeQueueRoleForRead
  - deadline:AssumeQueueRoleForUser
  - datazone:GetEnvironmentCredentials
  - elemental-inference:ExportDictionaryEntries
  - elemental-inference:GetMetadata
  - ec2:DescribeLaunchTemplateVersions
  - ec2:DescribeInstanceAttribute
  - ebs:GetSnapshotBlock
  - ec2:GetPasswordData
  - ecr:GetDownloadUrlForLayer
  - ecs:DescribeTaskDefinition
  - ecs:DescribeTasks
  - emr-serverless:GetApplication
  - emr-serverless:GetJobRun
  - events:ListTargetsByRule
  - execute-api:Invoke
  - glue:GetConnection
  - glue:GetJob
  - glue:GetWorkflowRunProperties
  - geo:GetDevicePosition
  - geo:GetDevicePositionHistory
  - greengrass:GetComponentVersionArtifact
  - healthlake:ReadResource
  - healthlake:SearchEverything
  - healthlake:SearchWithGet
  - healthlake:SearchWithPost
  - imagebuilder:GetComponent
  - iot:GetThingShadow
  - iot:Publish
  - iot:StartCommandExecution
  - iotjobsdata:DescribeJobExecution
  - iotjobsdata:StartNextPendingJobExecution
  - iotjobsdata:UpdateJobExecution
  - ivs:BatchGetStreamKey
  - ivs:CreateStreamKey
  - ivs:GetStreamKey
  - ivs:UpdateChannel
  - lakeformation:PutDataLakeSettings
  - m2:GetSignedBluinsightsUrl
  - mediaconnect:AddFlowOutputs
  - mediaconnect:DescribeFlowSourceThumbnail
  - mediaconnect:UpdateFlowOutput
  - medialive:DescribeThumbnails
  - medialive:UpdateChannel
  - mediapackagev2:PutChannelPolicy
  - mediapackagev2:PutOriginEndpointPolicy
  - notifications:GetManagedNotificationEvent
  - notifications:ListManagedNotificationEvents
  - mq:CreateUser
  - mq:UpdateUser
  - iotwireless:GetWirelessDevice
  - iotsitewise:BatchGetAssetPropertyAggregates
  - iotsitewise:BatchGetAssetPropertyValue
  - iotsitewise:BatchGetAssetPropertyValueHistory
  - iotsitewise:GetAssetPropertyAggregates
  - iotsitewise:GetAssetPropertyValue
  - iotsitewise:GetAssetPropertyValueHistory
  - iotsitewise:GetInterpolatedAssetPropertyValues
  - ivschat:CreateChatToken
  - kinesis:GetRecords
  - kinesisanalytics:DescribeApplication
  - kinesisvideo:GetClip
  - kinesisvideo:GetDASHStreamingSessionURL
  - kinesisvideo:GetHLSStreamingSessionURL
  - kinesisvideo:GetImages
  - kinesisvideo:GetMedia
  - kinesisvideo:GetMediaForFragmentList
  - medical-imaging:GetImageFrame
  - medical-imaging:GetImageSetMetadata
  - mediapackage:RotateChannelCredentials
  - mediapackage:RotateIngestEndpointCredentials
  - neptune-db:GetStreamRecords
  - neptune-db:ReadDataViaQuery
  - neptune-graph:ReadDataViaQuery
  - omics:GetReadSet
  - invoicing:BatchGetInvoiceProfile
  - invoicing:GetInvoicePDF
  - invoicing:ListInvoiceSummaries
  - pipes:DescribePipe
  - profile:SearchProfiles
  - rum:GetAppMonitorData
  - sdb:GetAttributes
  - sdb:Select
  - s3:GetDataAccess
  - s3vectors:GetVectors
  - s3express:CreateSession
  - sagemaker:DescribeModel
  - sagemaker:DescribeTrainingJob
  - scheduler:GetSchedule
  - servicediscovery:RegisterInstance
  - ses:GetSuppressedDestination
  - ses:GetEmailTemplate
  - ses:ListSuppressedDestinations
  - sns:ListSubscriptions
  - sns:ListSubscriptionsByTopic
  - sqs:ReceiveMessage
  - ssm:GetDocument
  - ssm:GetOpsItem
  - ssm:GetParameterHistory
  - states:DescribeStateMachine
  - states:DescribeExecution
  - states:GetActivityTask
  - states:GetExecutionHistory
  - sts:GetDelegatedAccessToken
  - logs:FilterLogEvents
  - logs:GetLogRecord
  - logs:GetQueryResults
  - sts:GetFederationToken
  - sts:GetServiceBearerToken
  - tax:GetTaxRegistration
  - wisdom:GetContent
  - tax:ListTaxRegistrations
  - textract:GetDocumentAnalysis
  - textract:GetDocumentTextDetection
  - textract:GetExpenseAnalysis
  - transcribe:GetTranscriptionJob
  - transfer:ImportSshPublicKey
  - translate:GetParallelData
  - translate:GetTerminology
  - vpc-lattice-svcs:Invoke

## =====================
## MEDIUM RULES
## =====================

## Regex patterns (case-insensitive) for "benign writes" that should be `medium`.
## Used to downgrade operational write actions (metrics/telemetry/etc.) from the default `high`.
benign_write_medium_action_regex:
  # X-Ray: telemetry/logging ingestion is not a direct privilege boundary.
  - ^xray:PutTraceSegments$
  - ^xray:PutTelemetryRecords$
  # CloudWatch Logs: retention configuration is operational, not a privilege boundary.
  - ^logs:PutRetentionPolicy$
  - ^cloudwatch:PutMetricData$
  - ^cloudwatch:PutMetricAlarm$
  - ^cloudwatch:PutCompositeAlarm$
  - ^cloudwatch:PutAnomalyDetector$
  - ^cloudwatch:PutInsightRule$
  - ^cloudwatch:PutDashboard$
  - ^cloudwatch:SetAlarmState$
  - ^cloudwatch:PutManagedInsightRules$
  - ^cloudwatch:CreateServiceLevelObjective$
  - ^cloudwatch:UpdateServiceLevelObjective$
  - ^[^:]+:PutMetricData$
  - ^logs:PutMetricFilter$
  - ^s3:PutMetricsConfiguration$
  - ^mediastore:PutMetricPolicy$
  - ^autoscaling:PutScalingPolicy$
  - ^[^:]+:PutScalingPolicy$
  - ^autoscaling:PutScheduledUpdateGroupAction$
  - ^autoscaling:BatchPutScheduledUpdateGroupAction$
  - ^autoscaling:PutNotificationConfiguration$
  - ^autoscaling:PutLifecycleHook$
  - ^autoscaling:PutWarmPool$
  - ^application-autoscaling:PutScalingPolicy$
  - ^application-autoscaling:PutScheduledAction$
  - ^application-autoscaling:RegisterScalableTarget$
  - ^application-autoscaling:DeregisterScalableTarget$
  - ^autoscaling-plans:CreateScalingPlan$
  - ^cur:PutReportDefinition$
  - ^cur:ModifyReportDefinition$
  - ^cur:PutClassicReportPreferences$
  - ^cur:ValidateReportDestination$
  - ^aps:PutAnomalyDetector$
  - ^[^:]+:PutAlarm$

## =====================
## LOW RULES
## =====================

## Exact action names that must always be classified as `low`.
## Useful for overriding generic heuristics.
low_exact:
  # Explicit overrides for read-only permissions that otherwise match
  # generic sensitive-read substrings (e.g. "Configuration", "Authorization")
  # or fall through to the unknown-default (e.g. BatchGet* verbs).
  - aoss:BatchGetCollection
  - amplifybackend:GetToken
  - aoss:BatchGetLifecyclePolicy
  - appconfig:ListConfigurationProfiles
  - appconfig:ListHostedConfigurationVersions
  - appfabric:GetAppAuthorization
  - appfabric:ListAppAuthorizations
  # GetQueryResults still requires access to the S3 result object.
  - athena:GetQueryResults
  - cognito-idp:GetSigningCertificate
  - cognito-identity:GetOpenIdToken
  - dynamodb:DescribeEndpoints
  - elasticbeanstalk:DescribeApplicationVersions
  - elasticbeanstalk:DescribeApplications
  - elasticbeanstalk:DescribeEnvironments
  - elasticbeanstalk:DescribeEvents
  - elasticbeanstalk:ListAvailableSolutionStacks
  - elasticbeanstalk:ListPlatformVersions
  - kinesisvideo:GetIceServerConfig
  - kinesisvideo:ListFragments
  # Iterator creation alone does not return stream records.
  - kinesis:GetShardIterator
  - logs:CreateLogGroup
  - logs:CreateLogStream
  - logs:PutLogEvents
  - route53:GetGeoLocation
  - route53:ListGeoLocations
  - synthetics:GetCanary
  - sts:GetCallerIdentity
  - sts:GetSessionToken
  # Metadata only. Document bytes require GetDocumentVersion with Fields=SOURCE.
  - workdocs:GetDocument

## =====================
## HEURISTICS / SUPPORTING LISTS
## =====================

## Global heuristic for write-like actions:
## - If the verb is "write-like" AND does NOT match `dangerous_write_regex`, it will be downgraded to `medium`.
## - Dangerous writes stay `high` (or `critical` if they match the critical rules above).
## Case-insensitive.

## Regex that defines which verbs count as "write-like".
write_like_prefix_regex: "^(Put|Create|Update|Modify|Delete|Set|Enable|Disable|Generate|Start|Stop|Cancel|Associate|Disassociate|Register|Deregister|Attach|Detach|Add|Remove|Reset|Run|Invoke|Import|Export|Upload|Download|Connect|Login|Reset|Rotate)"

## If this regex matches the verb, the write is considered dangerous and is NOT downgraded.
dangerous_write_regex: "(Policy|ResourcePolicy|RepositoryPolicy|BucketPolicy|RegistryPolicy|AccessPointPolicy|Role|User|Group|Permission|PassRole|Assume|Secret|Credential|Password|Token|Authorization|AccessKey|KeyPolicy|Decrypt|Encrypt|Grant|LoginProfile|SSHPublicKey|SigningCertificate|InstanceProfile|LaunchConfiguration|LaunchTemplate|UserData|Destination|Subscription|Target|Endpoint|Rule|Terminate|\\bKey\\b|Bucket|Object|Repository|Code)"

## Substrings (case-insensitive) that indicate a read action is likely to expose secrets/data.
## If a read verb contains any of these, it is upgraded from `low` to `high`.
sensitive_read_substrings:
  - Secret
  - Password
  - Credential
  - AccessKey
  - Token
  - PrivateKey
  - Certificate
  - Decrypt
  - Plaintext
  - GetAuthorizationToken
  - GetSecretValue
  - GetParameters
  - GetParameter
  - GetRepositoryPolicy
  - UserData
  - DistributionConfig

## A public certificate is metadata rather than secret key material. Specific
## certificate/private-key retrieval APIs can still be listed explicitly.
ignored_sensitive_read_substrings:
  - Certificate

## IAM verbs that represent privilege escalation / boundary changes and should be `critical`.
## IAM actions not matched here are typically `high` unless they're read-only.
iam_critical_verbs:
  - AddRoleToInstanceProfile
  - AddUserToGroup
  - AttachGroupPolicy
  - AttachRolePolicy
  - AttachUserPolicy
  - CreateAccessKey
  - CreateLoginProfile
  - CreatePolicyVersion
  - CreateServiceSpecificCredential
  - DeleteRolePermissionsBoundary
  - DeleteUserPermissionsBoundary
  - DetachGroupPolicy
  - DetachRolePolicy
  - DetachUserPolicy
  - PutGroupPolicy
  - PutRolePermissionsBoundary
  - PutRolePolicy
  - PutUserPermissionsBoundary
  - PutUserPolicy
  - ResetServiceSpecificCredential
  - SetDefaultPolicyVersion
  - UpdateAccessKey
  - UpdateAssumeRolePolicy
  - UpdateLoginProfile

## Verb prefixes treated as read-only (`low`) by default.
## Case-insensitive.
## NOTE: `BatchGet*` is already treated as read in code, so keeping `BatchGet` here is redundant.
read_prefixes: [Get, List, Describe, View, BatchList, BatchGet, Head]

## Verb prefixes treated as `medium` by default.
## Case-insensitive.
medium_prefixes:
  - Delete
  - BatchDelete
  - Remove
  - Destroy
  - Terminate
  - Suspend
  - Pause
  - Start
  - Stop
  - Cancel
  - Request
  - Reboot
  - Restart
  - Resume
  - Enable
  - Disable
  - Register
  - Unassign
  - Deregister
  - Open
  - Validate
  - Acknowledge
  - Fail
  - AssignPrivate
  - Abort

## Verb prefixes treated as `high` by default.
## Case-insensitive.
high_prefixes:
  - Put
  - Create
  - Update
  - Write
  - Run
  - Invoke
  - Attach
  - Detach
  - Modify
  - Associate
  - Disassociate
  - Authorize
  - Revoke
  - Upload
  - Download
  - Import
  - Export
  - Connect
  - Login
  - Reset
  - Rotate
  - Generate
  - Assume
  - Tag
  - Untag

## Verbs that directly set/attach resource policies (always `critical`).
resource_policy_verbs:
  - PutBucketPolicy
  - PutAccessPointPolicy
  - PutMultiRegionAccessPointPolicy
  - SetRepositoryPolicy
  - PutRepositoryPolicy
  - PutRegistryPolicy
  - PutResourcePolicy

## Suffixes that indicate policy-changing verbs (combined with prefixes below) => `critical`.
resource_policy_suffixes:
  - ResourcePolicy
  - RepositoryPolicy
  - BucketPolicy
  - RegistryPolicy
  - AccessPointPolicy

## Prefixes that, together with the suffix list above, imply a policy change => `critical`.
resource_policy_prefixes: [Put, Set]
