# AWS cross-service security review tracker

This queue covers trust paths that a service-only inventory misses. A row is a
hypothesis until a least-privilege principal, a permission-removed control, an
observed security effect, and complete cleanup satisfy the evidence gate in
`AWS-service-security-review.md`.

Status values are `queued`, `in_progress`, `validated`, `negative`, `blocked`,
and `conditional`. `Negative` means the tested generic claim did not work; it does
not mean every application-specific composition is safe.

## Mutable code and artifact consumers

| ID | Producer permission or surface | Consumer | Hypothesis | Status |
| --- | --- | --- | --- | --- |
| X001 | `s3:PutObject` | CloudFormation nested stack | Replacing a known mutable child template is applied by a later legitimate parent update under its stored service role. | validated |
| X002 | `ssm:PutParameter` | CloudFormation | Poison an `AWS::SSM::Parameter::Value<String>` value used by an IAM-sensitive property; a later update resolves the latest version. | validated |
| X003 | `glue:StartJobRun` arguments | Glue execution role | Override script or dependency locations without `iam:PassRole`. | validated |
| X004 | `s3:PutObject` | Glue dependency archive | A later trusted Glue run imports a replaced dependency under its execution role. | validated |
| X005 | `s3:PutObject` | CodeBuild | Replace a complete S3 source ZIP; a later trusted build executes its buildspec. | validated |
| X006 | `s3:PutObject` | CodePipeline and CodeBuild | Replace the version consumed by a later trusted pipeline execution. | validated |
| X007 | `s3:PutObject` | SSM `AWS-RunRemoteScript` | Replace a known script before a trusted command or State Manager run executes it on a managed node. | validated |
| X008 | `s3:PutObject` | MWAA | Exact-key overwrite of an existing DAG was automatically imported and executed by the scheduler as the MWAA execution role, without a DAG run or MWAA permission. | validated |
| X009 | `ecr:PutImage` and upload actions | App Runner auto deployment | Repoint a mutable tag and observe automatic deployment with the App Runner role. | validated |
| X010 | `ecr:PutImage` and upload actions | CodeBuild custom image | Repoint a mutable tag and observe the next ordinary build. | validated |
| X011 | `ecr:PutImage` and upload actions | AWS Batch | The next ordinary submission resolved the replacement tag; the image push itself did not create a job. | validated |
| X012 | `ecr:PutImage` and upload actions | ECS | Default task replacements stayed on the recorded digest; force-new-deployment resolved the new tag. | conditional |
| X013 | ECR publish/retag actions | EKS image consumer | After an ECR-only publisher moved a mutable tag V1→V2, `Always` on a warm node and `IfNotPresent` on a new node ran V2; `IfNotPresent` on the V1-warm node and digest pinning ran V1, while existing Pods remained unchanged. | validated |
| X014 | `ecr:BatchGetImage` + `ecr:PutImage` on a mutable tag | SageMaker Processing | An ECR-only publisher moved an existing manifest from V1 to V2; a later otherwise identical tag-based Processing job ran V2 under the fixed role, while a concurrent digest-pinned control still ran V1. | validated |
| X015 | `ecr:PutImage` and upload actions | Lambda container image | Lambda remains digest-pinned when a tag moves; a later `UpdateFunctionCode` using the same tag URI is the refresh boundary that resolves the replacement image. | conditional |
| X016 | `codeartifact:PublishPackageVersion` | CodeBuild/package client | A floating constraint selected the attacker version; an exact version pin remained on the trusted package. | validated |
| X017 | `codecommit:GitPush` | CodePipeline/CodeBuild | A source-change event ran the pushed revision automatically; with polling disabled, a later admin start consumed it. | validated |
| X018 | `s3:PutObject` | EMR bootstrap/step source | Exact-key overwrite was consumed by a later cluster bootstrap or added step under the EC2 instance role. | validated |
| X019 | `s3:PutObject` | SageMaker source bundle | Exact-key training and processing source replacements ran on later jobs under the SageMaker execution role. | validated |
| X020 | `s3:PutObject` | ECS FireLens | Exact-key put-only access replaced an unversioned S3 custom configuration; a later ECS/EC2 task using the unchanged definition rerouted its sensitive stdout from the benign group to the capture group. | validated |
| X021 | `s3:PutObject` | Elastic Beanstalk | Distinguish cached application versions from bundles fetched during rebuild/deployment. | conditional |
| X022 | `s3:PutObject` | CodeDeploy | Replace a revision consumed by a later deployment to execute lifecycle hooks on targets. | validated |
| X023 | package/image publication | Image Builder | A newer compatible component version was selected by an unchanged wildcard recipe and executed under the build instance profile. | validated |

## Stored service roles and delegated execution

| ID | Entry action | Consumer identity | Hypothesis | Status |
| --- | --- | --- | --- | --- |
| X024 | `scheduler:UpdateSchedule` / `CreateSchedule` | Scheduler execution role | Both APIs required `iam:PassRole`, including reuse of the current role; neither schedule permission is a standalone escalation. | conditional |
| X025 | `pipes:UpdatePipe` / `StartPipe` | EventBridge Pipes role | `StartPipe` reused the stored role; `UpdatePipe` required the role ARN and `iam:PassRole`. | validated |
| X026 | `states:StartExecution` | Step Functions role | Attacker-controlled input reached a preconfigured privileged task under the state-machine role. | validated |
| X027 | `appflow:StartFlow` | Preconfigured AppFlow authority | Start-only copied a private S3 source to the existing destination without source read or PassRole; disclosure still requires destination access. | validated |
| X028 | `transfer:StartFileTransfer` | Transfer connector access role and credentials | Start-only made the retained role read a private S3 object and deliver its exact bytes to the configured SFTP peer without caller S3/secret access or PassRole. | validated |
| X029 | `dms:StartReplicationTask` | Stored endpoint role and protected source/readable sink | Exact-task start access launched a ready full-load task; DMS copied the protected S3 canary through its stored role to the separately readable target without DMS reads/modification, source access, IAM, or PassRole. | validated |
| X030 | `elasticmapreduce:AddJobFlowSteps` | EMR EC2 instance profile | Exact add-step access injected a `command-runner.jar` shell step and wrote proof as the existing cluster's instance profile without PassRole. | validated |
| X031 | `batch:SubmitJob` | Batch job role | Exact-resource submit-only access replaced the container command and executed as the existing job role without `iam:PassRole`. | validated |
| X032 | SageMaker job start/create actions | SageMaker execution role | CreateTraining/Processing-style APIs enforce PassRole for their supplied role; `StartPipelineExecution` separately reused the pipeline's stored role and accepted attacker-controlled parameters. | validated |
| X033 | `ssm:StartAutomationExecution` | Stored Automation role | Execute a preauthorized runbook without caller `iam:PassRole`. | validated |
| X034 | `fis:StartExperiment` | Stored FIS role | Run an attacker-modified experiment template under its configured role. | validated |
| X035 | `servicecatalog:ProvisionProduct` | Launch constraint role | Execute an attacker-controlled provisioning artifact under a stored launch role. | validated |
| X036 | `datasync:StartTaskExecution` | DataSync role | Copy protected source data through an existing task without caller `iam:PassRole`. | validated |
| X061 | `secretsmanager:RotateSecret` | Configured rotation Lambda | Trigger the existing function through the Secrets Manager service without caller `lambda:InvokeFunction`; impact depends on the fixed rotation code and role. | conditional |
| X063 | `codepipeline:StartPipelineExecution` variables | Pipeline and downstream action roles | Override a V2 pipeline variable that reaches a privileged CodeBuild/deployment action without update, build-start, or PassRole permissions. | validated |
| X065 | `lambda:PutProvisionedConcurrencyConfig` | Lambda initialization code and execution role | Allocate a published version and trigger module/static initialization without `lambda:InvokeFunction`; impact depends on initialization side effects. | conditional |
| X066 | `lambda:UpdateFunctionEventInvokeConfig` plus destination read | Lambda asynchronous destination | Redirect future request/response records to a readable SQS queue through the function role without invoke, code update, or PassRole. | validated |
| X067 | `states:SendTaskSuccess` plus a task-token source | Step Functions role | Submit callback output that a fixed subsequent service integration consumes as privileged parameters without workflow start/update permissions. | validated |
| X068 | `events:UpdateApiDestination` | EventBridge Connection authentication | Redirect an existing API Destination so its retained API-key/OAuth/basic authorization is sent to an attacker endpoint on the next legitimate matching event. | validated |

## Event and message injection

| ID | Injection permission | Consumer | Required proof | Status |
| --- | --- | --- | --- | --- |
| X037 | `sqs:SendMessage` | Lambda/event source mapping | A configured privileged consumer was triggered, but the impact depends on application interpretation. | conditional |
| X038 | `sns:Publish` | Lambda/SQS/HTTP subscription | A configured privileged consumer was triggered, but the impact depends on application interpretation. | conditional |
| X039 | `kinesis:PutRecord` | Lambda consumer | A configured privileged consumer was triggered, but the impact depends on application interpretation. | conditional |
| X040 | `dynamodb:PutItem` | Stream consumer | Put and update events triggered a privileged consumer; impact depends on application interpretation. | conditional |
| X041 | `s3:PutObject` or tagging | S3 notification/EventBridge target | A write can trigger the configured consumer, but security impact is conditional on fixed rules/code trusting attacker-controlled object metadata or content; injection alone is not generically High. | conditional |
| X042 | `cloudwatch:PutMetricData` | Alarm actions | A namespace-scoped writer crossed a threshold and triggered the configured privileged consumer. | conditional |
| X043 | `logs:PutLogEvents` | Metric filter and alarm | An exact-stream writer injected matching logs and triggered the configured privileged consumer. | conditional |
| X044 | `iot:Publish` | IoT rule actions | Exact-topic publish-only access fed an unchanged SQL rule and caused its preconfigured Lambda to write the injected canary under its execution role. | validated |
| X045 | `events:PutEvents` plus `PutTargets` where needed | EventBridge target | Invoke a resource-policy-authorized target without caller `iam:PassRole`. | validated |
| X046 | API/data-plane invocation | Lambda or workflow integration | Invocation reaches only the configured integration and its fixed authorization/input mapping; privilege impact is conditional on attacker-controlled input reaching a sensitive downstream operation. | conditional |
| X062 | `cloudwatch:SetAlarmState` | Existing alarm action | Exact-alarm state-only access forced `INSUFFICIENT_DATA` to `ALARM`; CloudWatch invoked the unchanged configured Lambda and its stored role without caller metric-write, describe, direct-invoke, sink-write, or PassRole access. | validated |
| X064 | `logs:PutSubscriptionFilter` | Lambda or cross-account Logs destination | Exact-group filter-only access forwarded a later independently produced matching record to a preauthorized fixed Lambda, whose stored role produced protected-canary proof without log read/write, invoke, data access, or PassRole. The destination-policy prerequisite was independently denied. | validated |

## Configuration, credentials, and cross-account policy pivots

| ID | Controllable surface | Consumer or boundary | Hypothesis | Status |
| --- | --- | --- | --- | --- |
| X047 | `secretsmanager:PutSecretValue` | CodeBuild/application | An exact secret writer changed the next build's resolved `SECRETS_MANAGER` environment value under the build role. | validated |
| X048 | `ssm:PutParameter` | CloudFormation/CodeBuild/application | An exact parameter writer changed both a later CloudFormation resolution and the next build's `PARAMETER_STORE` environment value. | validated |
| X049 | AppConfig version/deployment actions | Application fleet | `CreateHostedConfigurationVersion` poisoned an inactive immutable version; `StartDeployment` independently selected a pre-existing version for a fixed consumer. Impact remains configuration-specific. | validated |
| X050 | `glue:UpdateTable` location mutation | Athena/ETL/Lake Formation | Exact-table update-only access redirected an Athena sink; a later unchanged privileged `INSERT INTO ... SELECT ...` wrote the protected canary to the attacker-readable prefix. | validated |
| X051 | Exact-record `route53:ChangeResourceRecordSets` | Fixed privileged HTTP consumer | An updater with no Route 53 reads redirected a known A record; a separately invoked unchanged Lambda sent the exact protected canary and its execution-role identity to the replacement collector. Proper hostname-validated TLS without an attacker-valid certificate blocks this demonstrated path. | validated |
| X052 | KMS alias or grant mutation | Encrypted-data consumer | A fresh CMK cannot satisfy mandatory same-session deletion because KMS retains it in `PendingDeletion`; live mutation remains blocked until an authorized disposable key already exists. | blocked |
| X053 | `s3:PutAccessPointPolicy` | S3 Backup Access Point and protected recovery data | A denied-before exact role installed a self-grant on a disposable Backup Access Point and read the protected S3 recovery-point canary without identity-based data access. | validated |
| X054 | `ecr:SetRepositoryPolicy` | Private image and tag operations | An exact-repository setter self-granted pull/write actions, extracted a protected canary from a known image layer through raw signed APIs without an authorization token or listing access, and created a new tag with `PutImage`; downstream consumer execution remains conditional and was not claimed. | validated |
| X055 | `sns:SetTopicAttributes` / `sqs:SetQueueAttributes` | Same-account principal and future messages | Each exact setter independently replaced its resource policy, granted the restricted principal subscribe/receive access, and exposed seeded or later-published canaries. | validated |
| X056 | `lambda:AddPermission` on a pre-existing Function URL | External unsigned caller | Exact-function AddPermission-only access installed both URL-only public statements; unsigned HTTP changed from 403 to 200 and the fixed function returned a protected canary plus its role identity while direct invocation remained denied. | validated |
| X057 | RAM share association/acceptance | External account | Usable cross-account access needs a second authorized principal/resource owner; the single-account lab has no safe independent boundary, so this remains blocked rather than claimed. | blocked |
| X058 | Backup copy/restore policy chain | External account/Region | Cross-account copy needs a second authorized destination account plus vault/KMS policy; the current one-account lab cannot establish that boundary. | blocked |
| X059 | CloudFormation StackSets | Organization/member execution roles | The lab organization contains only its management account, so no independent member-account execution role exists for a cross-account proof. | blocked |
| X060 | Bedrock agent invocation/input | Action-group Lambda/service role | Model input can reach only the particular tools and confirmation policy configured by the application; generic privilege impact is conditional on that agent's action groups and prompt/tool validation. | conditional |
| X069 | `events:UpdateConnection` | Stored OAuth client secret | Endpoint-only OAuth update was rejected because client parameters were required; omitting only the secret then failed on direct Secrets Manager authorization instead of disclosing it. | negative |
| X070 | `cognito-idp:UpdateIdentityProvider` | Stored OIDC client secret | A partial OIDC issuer redirect required all non-secret provider fields, and omitting `client_secret` cleared it rather than forwarding the previous value to the new token endpoint. | negative |
| X071 | `athena:UpdateWorkGroup` | Future Athena query principals | Redirect an enforced workgroup's later query results to attacker-readable S3 without query execution, result read, or PassRole permission. | validated |
| X072 | `glue:UpdateConnection` | Stored JDBC password | Changing only a JDBC URL, or URL plus username, failed connection-property validation; the existing password was not silently reused or sent elsewhere. | negative |
| X073 | `firehose:UpdateDestination` | Stored HTTP endpoint access key and future records | URL-only update retained the hidden access key and sent both it and the next independently produced record to the replacement HTTPS endpoint. | validated |
| X074 | `codepipeline:PutApprovalResult` | Downstream pipeline action and roles | Exact action-scoped access plus a live approval token bypassed the manual gate and caused the unchanged pipeline to invoke its privileged Lambda action. | validated |
| X075 | `cloudtrail:UpdateTrail` | Future audit-log delivery | Exact update-only access redirected a running trail; CloudTrail delivered a unique later canary event and other audit records to attacker-readable S3. | validated |
| X076 | `sagemaker:StartPipelineExecution` parameters | SageMaker pipeline role and fixed steps | Start-only access injected a parameter into an existing Lambda step, which processed it and wrote proof under its execution role without PassRole or direct invocation. | validated |
| X077 | `appsync:UpdateResolver` | Existing AppSync data-source role | Update-only access rewrote a resolver so the unchanged DynamoDB data source returned a protected canary through a separate existing API-key query path. | validated |
| X078 | `codepipeline:PutJobSuccessResult` | Downstream pipeline actions and roles | With a known acknowledged custom-job ID, result-only access falsely completed a zero-output validation gate and caused the unchanged pipeline to invoke its downstream Lambda role. | validated |
| X080 | `codepipeline:PutThirdPartyJobSuccessResult` | Downstream third-party pipeline actions | A customer pipeline accepted a pre-registered third-party gate, but even the account administrator could not poll its job because provider access is separately whitelisted; exposed execution IDs were not job IDs and result calls did not advance the gate. | negative |
| X081 | `glue:StartWorkflowRun` run properties | Existing Glue job role and trusted property sinks | Start-only access replaced a per-run property consumed by the unchanged job, which wrote proof under its execution role without PassRole or direct sink access. | validated |
| X082 | `transfer:UpdateConnector` | Secrets Manager SFTP credential and partner data | URL-only update retained the connector's role, secret reference, and trusted host key; a later connection sent the exact stored username/password to the redirected endpoint. | validated |
| X083 | `apigateway:PATCH` on an HTTP API integration | Retained API Gateway integration role and replacement backend | URI-only update preserved `CredentialsArn`; the next unsigned request invoked a preauthorized privileged Lambda that the caller could not invoke directly. | validated |
| X084 | `elasticloadbalancing:ModifyRule` | Existing target group and matching application traffic | Exact-rule update redirected only the matching request path to a pre-registered capture target; a separate POST delivered its protected header and body to the target Lambda role. | validated |
| X085 | `athena:UpdatePreparedStatement` | Future prepared-statement caller and its Glue/S3 access | Exact-workgroup update-only access replaced stored SQL without statement read, query execution, source access, or PassRole; a separate query role then executed the unchanged name and returned the protected S3 canary. | validated |
| X086 | `config:PutDeliveryChannel` | AWS Config inventory and future resource changes | Exact update-only access replaced the delivery S3 bucket and added an attacker-controlled SNS stream; a later independent resource change delivered the complete configuration item without Config/source/sink-write/PassRole permissions. | validated |
| X087 | `databrew:StartJobRun` | Stored DataBrew role, protected dataset, and fixed output | Exact-job start-only access ran the stored recipe and copied a protected S3 canary to the fixed output without job description, source/output access, IAM inspection, or PassRole; disclosure requires a separately readable sink. | validated |
| X088 | `athena:UpdateNamedQuery` | Saved-query consumer and its Glue/S3 authority | Exact-workgroup update-only access poisoned stored SQL without named-query read, execution, source access, or PassRole; an independent role fetched the same query ID and returned the protected canary. | validated |
| X089 | `athena:UpdateNotebook` | Future Spark notebook execution role | Exact-workgroup update-only access replaced a known inactive notebook without export/list/session/S3 access or PassRole; an independent victim later ran the stored cell under the notebook role and copied the protected canary. | validated |
| X090 | `lambda:UpdateEventSourceMapping` | Protected SQS/stream source and replacement Lambda | Exact-mapping update-only access changed and enabled a disabled SQS mapping; Lambda delivered an already-queued protected canary to a pre-existing replacement function whose role already authorized the source. | validated |
| X091 | `glue:UpdatePartition` | Partition-aware Athena/ETL writer | Exact-action access redirected a known partition; the next unchanged privileged write placed the protected canary in a separately readable prefix. The batch API unexpectedly authorizes with this same action. | validated |
| X092 | `cognito-idp:UpdateUserPoolClient` | Confidential Cognito app client | Exact update-only access returned the existing client secret even though the caller was denied `DescribeUserPoolClient`; the returned value matched both the creation secret and administrator-only describe result. | validated |
| X093 | `apigateway:PATCH` (`UpdateApiKey`) | Existing REST API key | An exact-key update-only caller could change the key but the response omitted its value; unlike Cognito client updates, this was not a secret-read primitive. | negative |
| X094 | `s3:PutObject` | ECS `environmentFiles` consumer | Exact-key put-only access changed a security-relevant variable; a later unchanged Fargate task wrote protected data and its task-role identity to the substituted destination. | validated |
| X095 | `codepipeline:UpdatePipeline` | Existing pipeline role and replacement action | AWS required `iam:PassRole` on the unchanged stored service role, and root-only pipeline authorization also missed stage child ARNs; update-only access did not save a redirect or execute the target. | negative |
| X096 | `codebuild:RetryBuild` | Completed build configuration and CodeBuild service role | Exact-project retry-only access re-executed a known completed build under its stored role and recreated a protected canary output without StartBuild, build reads, data access, or PassRole. | validated |
| X097 | `states:RedriveExecution` | Failed Standard execution and its stored roles | Exact-execution redrive-only access resumed the same failed path after an independent dependency change; fixed Lambda code then copied a protected canary and its role identity without StartExecution, invoke, data access, or PassRole. | validated |
| X098 | `sagemaker:RetryPipelineExecution` | Failed pipeline execution and stored pipeline/downstream roles | Exact-execution retry-only access replayed the same failed path and original execution override; fixed Lambda code then copied a protected canary and role identity without pipeline start/update, invoke, data access, or PassRole. | validated |
| X099 | `codepipeline:RetryStageExecution` | Failed pipeline stage and existing action roles | Exact-stage retry-only access replayed failed Lambda actions after an independent gate change and produced the protected canary without pipeline reads/start/update, direct invocation, data access, or PassRole. | validated |
| X100 | `eks:AssociateIdentityProviderConfig` | Kubernetes OIDC authentication and pre-existing RBAC | Exact-cluster association-only access trusted an attacker-controlled issuer; its token changed from HTTP 401 to reading a protected Secret and creating cluster-scoped RBAC through a matching pre-existing `cluster-admin` group binding, without an EKS access entry or PassRole. | validated |
| X101 | `glue:ResumeWorkflowRun` | Failed workflow node and stored Glue job role | Exact-workflow resume-only access replayed a prior failed node after an independent dependency change; the fixed job copied a protected canary and its stored-role identity without Glue reads/start/update, data access, or PassRole. | validated |
| X102 | `cloudformation:ExecuteChangeSet` | Available change set and stack service role | Stack-ARN-scoped execute-only access ran a pre-staged update under the stored CloudFormation role, which granted exact-object read without CloudFormation reads/change creation/update, direct IAM, data access, or PassRole. | validated |
| X103 | `events:PutRule` | Existing disabled rule, target, and destination authorization | Exact-rule PutRule-only access enabled a schedule while preserving its separately attached Lambda target; EventBridge later invoked fixed code under the target role and produced the protected canary without PutTargets, PutEvents, invoke, data access, or PassRole. | validated |
| X104 | `s3:PutBucketNotification` | Pre-authorized Lambda and future independent object writes | Exact-bucket notification-only access installed a blind filtered route; a later administrator object write invoked unchanged Lambda code under its stored role and produced the protected canary. S3 atomically rejected the route before and after the exact Lambda destination grant. | validated |
| X105 | `lambda:CreateEventSourceMapping` | Existing source and fixed Lambda execution role | Function-conditioned creation-only access attached a known SQS queue; a later independent message made unchanged code use its stored role and produce protected-canary proof. The action requires Resource `*`, and AWS exposes no source-ARN condition key. | validated |
| X106 | `sns:Subscribe` | Existing topic, pre-authorized Lambda, and future independent publish | Exact-topic subscribe-only access auto-confirmed a known Lambda endpoint; a later administrator publish invoked unchanged code under its stored role. Without the Lambda topic/account resource-policy grant, subscription still confirmed but delivery produced no evidence. | validated |
| X107 | `events:PutPermission` | Custom event-bus policy and existing rule/target | Exact-bus policy-only access granted the same IAM user constrained PutEvents; its previously denied event then matched an unchanged rule and invoked a pre-authorized fixed Lambda. Removing the exact statement restored denial. | validated |
| X108 | `autoscaling:SetDesiredCapacity` | Existing launch template, instance profile, and boot workload | Exact-ASG scale-only access changed desired capacity from zero to one; Auto Scaling reused unchanged configuration and the EC2 instance role to produce protected-canary proof without EC2 RunInstances, template changes, data access, or PassRole. | validated |
| X109 | `lambda:PutFunctionEventInvokeConfig` / `UpdateFunctionEventInvokeConfig` | Source execution role and fixed asynchronous destination | Each exact-version action independently routed a later administrator async invocation to a fixed Lambda through the unchanged source role. Put created a config; Update required one and preserved unspecified retry fields. | validated |
| X110 | `ecs:UpdateService` desired count only | Dormant service task definition, task role, and network | Exact-service update-only access changed desired count zero→one; Fargate ran the unchanged task definition/image digest and fixed role, producing protected-canary proof without RunTask, registration, reads, data access, or PassRole. | validated |
| X111 | `events:StartReplay` | Archived events and unchanged EventBridge targets | Exact archive, replay, and source-bus authorization re-emitted a stored event to an unchanged rule/target; the fixed Lambda ran under its stored role and produced protected-canary proof without EventBridge reads, PutEvents, target mutation, direct invoke, data access, IAM, or PassRole. Empty and fresh-revoked controls failed. | validated |
| X112 | `config:StartConfigRulesEvaluation` | Periodic custom Config rule and fixed Lambda | Exact-rule evaluation-only access forced the unchanged custom evaluator to run under its stored Lambda role and produce protected-canary proof without Config reads/mutation, direct invoke, data access, IAM, or PassRole. Empty and fresh-revoked controls failed. | validated |
| X113 | `cloudformation:SignalResource` | Waiting stack template and stored service role | Exact-stack signal-only access satisfied a named WaitCondition; the unchanged stack continued into its fixed custom resource under stored stack/provider roles, while empty, wrong-logical-ID, and fresh-revoked controls failed. | validated |
| X114 | `codepipeline:EnableStageTransition` | Blocked execution and downstream action roles | Exact-stage enable-only access released an already waiting execution into an unchanged Lambda action, which copied the protected canary under its fixed role without pipeline reads/start/update, direct invocation, data access, or PassRole. | validated |
| X115 | `codepipeline:OverrideStageCondition` | Active stage condition and downstream action roles | Exact-stage override-only access abandoned an in-progress failing CloudWatch-alarm entry rule; the same execution entered the unchanged Lambda stage and copied the protected canary under its fixed role without pipeline reads/start/mutation or PassRole. | validated |
| X116 | `codepipeline:RollbackStage` | Prior successful pipeline revision and action roles | Exact-stage rollback-only access created a separate manual-rollback execution using the prior successful source artifact; the unchanged Lambda action ran under its stored role and reproduced protected-canary proof without pipeline reads/start/update, source/data access, direct invoke, IAM, or PassRole. Empty and fresh-revoked controls failed. | validated |
| X117 | `ssm:StartAssociationsOnce` | Existing State Manager association and managed-node identity | Exact-association start-once access executed unchanged shell commands on the fixed managed node; its instance role read the protected canary and wrote proof without SendCommand, association reads/mutation, node access, data access, IAM, or PassRole. Empty and fresh-revoked controls failed. | validated |
| X118 | `synthetics:StartCanary` | Existing stopped canary code and execution role | Exact-canary start-only access ran unchanged code under the stored canary execution role and produced protected-canary proof without canary reads/update/dry-run preparation, direct Lambda access, data access, IAM, or PassRole. Empty and fresh-revoked controls failed. | validated |
| X119 | `glue:BatchCreatePartition` | Partition-aware Athena/ETL reader | Exact catalog/database/table action added a partition pointing to pre-existing attacker content; a later independent full-table Athena query returned the injected marker beside the legitimate row without candidate Glue reads, Athena, S3, IAM, or PassRole access. | validated |
